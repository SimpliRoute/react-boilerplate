import { createStyles } from '@material-ui/core';

const styles = () =>
    createStyles({
        root: {
            paddingTop: 50,
            paddingLeft: 10
        }
    });

export default styles;
